<div id="hello-world"></div>

<br/>
<br/>

<h1>Here's some images we know about:</h1>

<table class="table">
  <tr>
    <th>ID</th>
    <th>S3 URL</th>
    <th>Taken At</th>
    <th>Blueness Index</th>

  <%= for image <- @images do %>
    <tr>
      <td><%= image.id %></td>
      <td><a href="<%= image.s3_url %>"><%= image.s3_url %></a></td>
      <td><%= image.taken_at %></td>
      <td><%= image.blueness_index %></td>
    </tr>
  <% end %>
</table>
